## How to Install

-   Clone this repository
-   Rename `.env.example` to `.env`
-   Create database named `stisla`
-   Run this command

```
# Installing dependencies
$ composer install

# Serve
$ php artisan serve
```
